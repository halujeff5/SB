function countDown(num){
    for (num = num; num>0; num --)
    {console.log(num)}
    console.log("DONE!")
}

function randomGame(){

for (let i = 0; i >=0; i++) {let select = setInterval(Math.random(1), 1000)
    }
    if (select > .75) {
        console.log(i)}
    else {console.log(select)}
    }

